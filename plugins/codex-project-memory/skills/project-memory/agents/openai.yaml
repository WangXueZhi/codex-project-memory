interface:
  display_name: "Project Memory"
  short_description: "Recall traceable project knowledge within a clear token budget"
  default_prompt: "Use $project-memory to recall only the project knowledge relevant to this task, then deep-read the recommended memories within budget."
policy:
  allow_implicit_invocation: true
