interface:
  display_name: "Project Memory"
  short_description: "Maintain traceable memory and guide users through offline project knowledge"
  default_prompt: "Use $project-memory to load this project's durable context, summarize knowledge gaps, and open its guided offline knowledge graph when useful."
policy:
  allow_implicit_invocation: true
